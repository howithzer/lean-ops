[pytest]
testpaths = scripts/glue/tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short

# Markers for test categories
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (may need mocking)
    slow: Slow tests (Spark-based)
