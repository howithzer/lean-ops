openapi: "3.0.0"
info:
  title: Trades Topic Contract - BROKEN
  version: "BAD"
# Scenario 05a - SHOULD FAIL: field removed
# ssn has been removed from the schema.
# Expected result: FAIL - FIELD REMOVED: ssn

components:
  schemas:
    Trade:
      required:
        - trade_id
        - trader_name
        - trade_ts
      properties:
        trade_id:
          type: string
        trader_name:
          type: string
        amount:
          type: number
          format: double
        currency:
          type: string
        credit_card:
          type: string
        trade_ts:
          type: string
          format: date-time
